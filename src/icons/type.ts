import { ColorValue } from "react-native";

export interface IIcon {
  width?: number | string;
  height?: number | string;
  color?: ColorValue;
  opacity?: number;
  rotation?: number;
  scale?: number;
  mode?: string;
  marginleft?: number;
}